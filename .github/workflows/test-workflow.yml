name: GitHub Actions Demo
run-name:  GitHub Actions EV demo
on: [push]
jobs:
  Explore-GitHub-Actions-EV:
    runs-on: ubuntu-latest
    steps:
      - name: List all default EVs
        run: printenv
      - name: access event  name
        run:
          echo "The event that trigger this workflow ${{ github.event_name }}"
          echo "Name of the branch ${{ github.ref_name }}"
